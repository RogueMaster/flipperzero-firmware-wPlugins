App(
    appid="biovault",
    name="BioVault",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="biovault_app",
    stack_size=4 * 1024,
    fap_category="NFC",
    fap_author="Shain Lakin",
    fap_version="0.1",
    fap_description="Read/write an enclave-encrypted vault on an NTAG I2C Plus 2K NFC implant (xSIID), Sector 1. Milestone 1: raw read of Sector 1 user memory.",
    requires=["gui", "notification"],
)
