#pragma once

#include <flipper_application/api_hashtable/api_hashtable.h>

/* Resolver for the app's private symbols; defined in seos_app_api_table.cpp. */
extern const ElfApiInterface* const application_api_interface;
