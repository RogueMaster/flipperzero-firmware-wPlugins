# ufbt build output
dist/
.ufbt/

# editor / tooling
.vscode/
.clang-format
compile_commands.json

# macOS
.DS_Store

# Catalog submission artifacts. These belong to the PR against
# flipper-application-catalog, not to the app itself, and the manifest embeds
# this repo's own commit SHA, which would be circular to track here.
catalog-manifest.yml
catalog-pr-body.md
