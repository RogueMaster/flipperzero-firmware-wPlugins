# uFBT build artifacts
app/.ufbt/
app/.vscode/
app/dist/
app/build/

# Compiled binaries
*.fap
*.elf
*.bin
*.hex
*.o
*.d

# IDE / editor
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code session data
.claude/
CLAUDE.md

# Reference dict files at the repo root (the runtime version is
# embedded in mfp_app.c and auto-deployed on first launch).
/mfp_default_keys.dic
/mfp_test_keys.dic

# Local working copy of the catalog manifest — the canonical version
# lives in the flipper-application-catalog fork.
/catalog-submission/
